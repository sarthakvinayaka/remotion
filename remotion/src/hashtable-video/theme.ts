// Re-export the shared code-video theme so this video reuses the exact same
// dark palette / fonts as the other code-typing videos in this project.
export { cv, cvFonts } from "../code-video/theme";
