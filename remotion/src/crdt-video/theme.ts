// Re-export the code-video theme so both video projects share one visual
// language (same fonts, same dark palette) without duplicating font loads.
export { cv, cvFonts } from "../code-video/theme";
